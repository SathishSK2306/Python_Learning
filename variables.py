"""
* variables are used to store some data's like a name is rajini, we need to store this, so we can use variable.

* name = "rajini" in this way we can store the value

* here name is "variable name" and "rajini" is value(data).

"""
number = 20 # here 20 has been stored in the variable of number.

"""
after we assign the variable what happening exactly ?

* whenever we store the the data inside the variable, it's split's the two different types of memory

* i) Variable Space(static memory) => here the variable name store with the value address
    like this, number | 0X11

* i) Value Space(heap memory) => here also the value has been stored with the value address
    like this, 0X11 | 20

"""

